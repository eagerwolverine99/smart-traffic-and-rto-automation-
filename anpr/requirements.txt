# ANPR Project — Python dependencies
# Install with:  pip install -r requirements.txt
# Tested on Python 3.10 and 3.11

# --- Core ---
opencv-python>=4.8.0
numpy>=1.24.0

# --- YOLO ---
ultralytics>=8.0.0

# --- OCR (preferred) ---
paddlepaddle>=2.5.0
paddleocr>=2.7.0

# --- OCR (fallback if PaddleOCR install fails) ---
# easyocr>=1.7.0

# --- Logging ---
openpyxl>=3.1.0
pillow>=10.0.0
